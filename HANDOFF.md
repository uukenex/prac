# 인수인계 노트 (다른 컴퓨터에서 이어받을 때 가장 먼저 확인)

이 파일이 존재하면, 세션 시작 시 (특히 "다른컴퓨터에서 작업한 것 확인해줘" 류 요청을 받으면)
**다른 무엇보다 먼저 이 내용부터 정리해서 사용자에게 알려줄 것.** 그대로 복붙하지 말고
그때그때 다시 말로 정리해서 전달. 확인/전달 후에도 파일은 지우지 말고, 다음 관련 작업이
실제로 끝나면(예: 아래 "다음 단계"가 완료되면) 그때 이 파일을 삭제하거나 내용을 갱신할 것.

## 2026-09-20~21 세션: 시즌5 밸런스 전면 재설계 (진행 중, 미완료)

**요청 배경**: "전체적으로 밸런스를 다시잡고싶다. 한 번의 전투가 오래가면 좋겠다(일반몬스터는
파티가 4~5라운드, 보스는 10라운드+ 걸리게). 1~100층 몬스터 공격력/체력/방어력을 계단식이
아니라 완전한 선형구조로. 보스는 조금 더 세도 됨. 장비/악세서리/동료 베이스 스탯도 이번에
같이 수치 재조정. 단, 기존 시스템이 갑자기 바뀌면 안 되고 실제 배포했을 때만 바뀌게."

**한 일**:
- 실제 유저 16명(11~89층)의 진짜 파티 스탯을 `computeEffectiveStat()` 공식 그대로
  재계산해서 실측 기반 캘리브레이션 데이터를 확보.
- 그 데이터로 몬스터 HP/ATK/DEF를 "일반 4.5라운드 처치 / 보스 11라운드 처치"가 되도록
  역산하고, 1~100층 전체를 관통하는 완전한 선형식 하나로 정리.
- 동료 등급 베이스(`GRADE_BASE`)와 장비 등급 보너스(`EQUIP_BONUS`)도 등급 간 배율이
  들쭉날쭉하던 것을 ★1/★6(또는 ★7) 실측치를 양끝으로 고정한 등비수열로 재설계.
  한계돌파(`LIMIT_BREAK_PCT`)는 이미 완전 등차수열이라 값은 그대로 두고 테이블로만 이전.
- 새 테이블 4개(`TBOT_S5_MONSTER_INFO_V2`, `TBOT_S5_GRADE_BASE_V2`,
  `TBOT_S5_EQUIP_BONUS_V2`, `TBOT_S5_LIMIT_BREAK_V2`) 생성 + 데이터 전량 INSERT하는 SQL을
  작성해서 커밋/푸시 완료: [`src/main/resources/ddl/S5_BALANCE_V2_LINEAR.sql`](src/main/resources/ddl/S5_BALANCE_V2_LINEAR.sql)
- 설계 근거/AS-IS→TO-BE 표 전체는 [`src/main/resources/ddl/S5_TOWER_DESIGN.md`](src/main/resources/ddl/S5_TOWER_DESIGN.md)의
  "[2026-09-21] 밸런스 전면 재설계" 항목에 기록됨.
- 계획 문서: `.claude/plans/linear-mapping-wall.md` (있다면 참고, 없어도 위 설계 근거로 충분).

**아직 안 한 것 (사용자가 "검토 및 실적용은 추후에" 라고 명시적으로 미룸)**:
1. 위 SQL을 실제 라이브 DB에 실행하는 것 — **아직 안 함**.
2. `BotS5ServiceImpl.java`가 새 테이블(V2)을 읽도록 코드 교체 — **아직 안 함**
   (`calcBaseStat`/`computeEffectiveStat`/`selectMonster`/`applyHardcoreFloorScale` 전부
   여전히 기존 방식 그대로 사용 중. 즉 지금 이 커밋을 배포해도 라이브 동작은 100% 그대로임).
3. 새 몬스터 곡선으로 실제 라운드 수가 목표(4~5/10+)에 맞는지 시뮬레이션 재검증.
4. 실측 플레이테스트로 최종 수치 조정.

**임의로 정한 값(사용자 최종 확인 필요)**: 목표 라운드 K=4.5(일반)/11(보스), 몬스터 방어력=
파티원 평균 데미지의 35%, 몬스터 공격력=동료 평균체력의 15%(일반)/20%(보스), 보스 DEF/ATK
기울기=일반의 1.2배. 이 값들에 대한 사용자 승인/조정 없이 그대로 다음 단계(실제 DB 반영+
코드 전환)로 넘어가지 말 것 — 사용자가 원하면 먼저 이 판단값부터 재확인.

**다음에 이어받으면 할 일(사용자가 "진행해줘" 하면)**: 위 "아직 안 한 것" 1~4번을 순서대로
진행. 시작 전에 `S5_TOWER_DESIGN.md`의 해당 항목과 `S5_BALANCE_V2_LINEAR.sql`부터 다시
읽고, 그 사이(이 노트를 쓴 뒤로) 사용자가 판단값을 바꿔달라고 한 게 없는지 먼저 확인.
