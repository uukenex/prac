# 인수인계 노트 (다른 컴퓨터에서 이어받을 때 가장 먼저 확인)

이 파일이 존재하면, 세션 시작 시 (특히 "다른컴퓨터에서 작업한 것 확인해줘" 류 요청을 받으면)
**다른 무엇보다 먼저 이 내용부터 정리해서 사용자에게 알려줄 것.** 그대로 복붙하지 말고
그때그때 다시 말로 정리해서 전달. 확인/전달 후에도 파일은 지우지 말고, 다음 관련 작업이
실제로 끝나면(예: 아래 "다음 단계"가 완료되면) 그때 이 파일을 삭제하거나 내용을 갱신할 것.

## 2026-09-20~21 세션: 시즌5 밸런스 전면 재설계 (실적용 완료, 미세조정만 남음)

**요청 배경**: "전체적으로 밸런스를 다시잡고싶다. 한 번의 전투가 오래가면 좋겠다(일반몬스터는
파티가 4~5라운드, 보스는 10라운드+ 걸리게). 1~100층 몬스터 공격력/체력/방어력을 계단식이
아니라 완전한 선형구조로. 보스는 조금 더 세도 됨. 장비/악세서리/동료 베이스 스탯도 이번에
같이 수치 재조정." → 이후 "v2로 진행되면서 전투화면도 내가 올린 사진처럼 바꾸자. 진행해줘."로
실적용 확정.

**완료된 것** (2026-09-21, 자세한 내용은 `S5_TOWER_DESIGN.md`의 "[2026-09-21] 실적용 + 전투화면
재설계 + 하루한도 전환" 항목):
1. `S5_BALANCE_V2_LINEAR.sql` 라이브 실행 완료 (`TBOT_S5_LIMIT_BREAK_V2`의 `LEVEL` 컬럼이
   Oracle 예약어라 `LB_LEVEL`로 수정 후 재적용, 나머지 3테이블은 최초 실행에 성공).
2. `BotS5ServiceImpl.java`가 V2 4테이블을 읽도록 전환 완료 (`loadBalanceV2()` +
   `BALANCE_V2_ENABLED=true` 오버레이 방식, `calcBaseStat`/`computeEffectiveStat`/
   `applyHardcoreFloorScale`). **문제 생기면 `BALANCE_V2_ENABLED = false`로 바꾸고
   재배포하면 즉시 V1으로 롤백된다.**
3. 하루 이동 한도를 "주사위(전투+이동 전체) 1200회"에서 "보드 이동만 400회(+카톡 100=500)"로
   전환 (`DAILY_MOVE_LIMIT`/`KAKAO_BONUS_MOVE`), 전투 턴은 무제한.
4. 전투화면을 삼국지 대전화면 스타일로 재설계(사용자 첨부 스샷 기반) -- 세력비교바/대치
   장면/LEVEL·POWER·GUARD·HIT%(항상 100%/100%) 스탯표.

**아직 안 한 것**:
1. 라운드 수 목표(일반 4~5, 보스 10+)가 실제로 맞는지 시뮬레이션 재검증.
2. 실측 플레이테스트로 최종 수치 조정 -- 특히 아래 임의값들.

**임의로 정한 값(사용자 최종 확인 필요, 이미 라이브 적용됨)**: 목표 라운드 K=4.5(일반)/
11(보스), 몬스터 방어력=파티원 평균 데미지의 35%, 몬스터 공격력=동료 평균체력의 15%(일반)/
20%(보스), 보스 DEF/ATK 기울기=일반의 1.2배. 실측 결과 너무 쉽거나 어려우면 이 값들부터
재조정(`S5_BALANCE_V2_LINEAR.sql`의 선형식 또는 K값을 바꿔 새 SQL로 재INSERT하거나 UPDATE).

**다음에 이어받으면 할 일**: 사용자가 실제로 몇 판 플레이해보고 "너무 쉽다/어렵다/딱 좋다"
피드백을 주면 그에 맞춰 위 임의값 조정. 전투화면 UI도 실제 사용해보고 추가 요청이 있을 수
있음(예: 몬스터 LEVEL을 층수 대신 다른 값으로, HIT%를 실제 회피율과 연동 등).
