# 인수인계 노트 (다른 컴퓨터에서 이어받을 때 가장 먼저 확인)

이 파일이 존재하면, 세션 시작 시 (특히 "다른컴퓨터에서 작업한 것 확인해줘" 류 요청을 받으면)
**다른 무엇보다 먼저 이 내용부터 정리해서 사용자에게 알려줄 것.** 그대로 복붙하지 말고
그때그때 다시 말로 정리해서 전달. 확인/전달 후에도 파일은 지우지 말고, 다음 관련 작업이
실제로 끝나면(예: 아래 "다음 단계"가 완료되면) 그때 이 파일을 삭제하거나 내용을 갱신할 것.

## 2026-09-24 확인: 이 파일 아래 내용은 대부분 지난(09-21/22) 기록, 최신 아님

`git log`로 09-21 이후 커밋 10개를 추가로 확인함(로그뷰어 페이지 신설/전면개편, 몬스터
공격력 즉효+근본 처방, 반격 방어력 차감 표기 등) -- 전부 push된 상태로 이 컴퓨터에도
정상 pull/컴파일 확인 완료. 아래 "보류 중"이라던 로스터리 DEF/투자배율 이슈는 **09-22에
이미 실제로 코드 반영됨**(`investAtkScale` 신설 -- 아래 내용은 과거형이라 그대로 둠, 최신
상태는 `S5_TOWER_DESIGN.md`의 09-22 항목들 참고). 아래 09-21 기록의 "다음에 확인해야 할 것"
(V2 파티로스터 스크린샷, 송곳 효과, 91층 진입)은 실제로 검증됐는지 이후 커밋 메시지에
명시적 확인 기록이 없어 **여전히 미확인 상태로 보임** -- 다음 세션에서 라이브로 직접
확인 권장.

## 2026-09-21 추가 세션: 다른 컴퓨터 작업과 병합(merge) 완료

이 컴퓨터에서 "60층 미만 방어력 0, 60층 이후 선형(61층≈100, 99층은 지금 그대로)" +
"전설제작 전체 오픈" 요청을 처리하던 중 `git push`가 non-fast-forward로 거부됨(다른
컴퓨터 세션이 그 사이 22개 커밋을 이미 push해뒀음 -- 아래 원래 있던 "2026-09-21 세션"
항목이 그 내용). `git merge origin/master`로 병합 -- `applyHardcoreFloorScale()`/
`craftLegendary()` 두 함수에서 충돌 발생(둘 다 이 컴퓨터와 다른 컴퓨터가 같은 함수를
건드림), 수동 해결 후 `mvn compile` 재검증 완료, push까지 마침. 병합하며 알게 된 것:
DEF 앵커값을 요청 시점엔 V1 원본(7500)으로 잡았었는데, 병합해보니 다른 컴퓨터가 이미
V2를 라이브로 돌려놔서 "지금 99층" 실제 값이 6096이었음 -- 앵커를 6096으로 정정해서
반영함(상세: `S5_TOWER_DESIGN.md`).

**다음에 이어받으면 가장 먼저 할 일은 이 컴퓨터(또는 배포 서버)에서 `git pull` 후
빌드+재배포해서 라이브를 이 병합 커밋까지 올리는 것** -- 아래 "지금 라이브가 뒤처진 상태"
항목(다른 컴퓨터 세션 기록, 여전히 유효) + 이번에 병합된 DEF/전설제작 변경 둘 다 재배포
전까지는 라이브에 반영 안 됨.

## 이 세션에서 새로 완료된 것 (병합 후 push됨)

1. **몬스터 방어력(DEF) 층별 선형화**: 60층 미만은 0, 61~99층은 100→6096으로 선형 증가
   (보스 포함, V1/V2 HP·ATK 경로와 무관하게 DEF는 항상 이 공식). HP/ATK는 안 건드림.
2. **전설제작 전체 오픈**: `craftLegendary()`에 남아있던 `NO_COOLDOWN_YN`(관리자/테스트
   계정 전용) 게이트 제거 -- 이제 조각 10개만 모으면 누구나 제작 가능.
   **→ 아직 라이브에서 실제로 일반 계정(예: 도륙이냥/달소)이 성공적으로 제작되는지
   재확인 안 함, 다음에 검증 필요.**

## (아래는 병합해 들어온 다른 컴퓨터 세션의 기존 기록, 여전히 유효)

## ⚠️ 최우선 확인: 라이브가 최신 커밋보다 뒤처져 있었음

세션 종료 시점에 라이브 서버(`rgb-tns.dev-apc.com`)가 아래 순서로 재배포됐었는데, **마지막
`70d7f43e`(V2 파티로스터 실제 수정)가 반영되기 전 상태에서 세션이 끝났다** -- 직접 브라우저로
떠 있는 JS를 열어서 확인함(`updateBattlePartyV1()` 호출 위치가 구버전 그대로였음). 이어받으면
**가장 먼저 재배포하고, 아래 "확인해야 할 것" 순서대로 눈으로 검증**할 것.

## ⚠️ 재발 방지: `@Scheduled` 메서드는 반드시 인터페이스에도 선언할 것

이번 세션에 `@Scheduled` 정리 작업(`cleanupJunkZeroFloorUsers`)을 `BotS5ServiceImpl`에만
추가했다가 **전 사이트가 다운되는 사고**가 있었다(JDK 동적 프록시라 인터페이스에 없는 메서드는
스케줄러가 호출 못 해서 `ContextLoaderListener` 예외로 컨텍스트 부팅 자체가 실패,
`IllegalStateException: Need to invoke method ... but not found in any interface(s)`).
앞으로 `BotS5ServiceImpl`에 `@Scheduled`/`@Async` 등 리플렉션으로 프록시 통해 호출되는
애노테이션 메서드를 추가할 땐 **반드시 `BotS5Service` 인터페이스에도 같이 선언**할 것
(`mvn -o -q compile`만으론 못 잡음 -- 인터페이스 누락은 컴파일 에러가 아니라 런타임 기동
실패라서). 상세: `S5_TOWER_DESIGN.md` "[2026-09-21 긴급수정]" 항목.

## 이번 세션에 완료된 것 (커밋 순서대로, 전부 push됨)

1. 전설제작 조각 10개 미만이면 버튼 비활성화 + `CUR_FLOOR=0` 미사용계정 매일 새벽4시 자동정리
   스케줄러(단, 위 사고로 인터페이스 누락 긴급수정 포함).
2. **★7 전설장비 고유이름 미표시 + 숨은 전투 버그**: `selectUserEquip`/`selectEquipByCompanion`
   둘 다 `LEGENDARY_ID`를 안 내려주고 있었음 -- 전자는 표시 문제("검으로만 나와"), 후자는 더
   심각하게 **송곳(DEF_STEAL) 효과가 실전투에서 한 번도 발동 안 하던 버그**였음. 둘 다 수정,
   프론트도 ★7 장비에 고유이름 표시하도록 반영. **→ 아직 라이브에서 실제로 송곳 착용 후 전투
   효과 발동 확인 안 함, 다음에 검증 필요.**
3. `/탑정보`(`/ㅌㅈㅂ`)에 전설의조각 보유량 표시 추가.
4. **91~99층(블록10) 오픈** -- `CONTENT_LOCKED_FLOOR` 91→100. 91층 이상은 계단마다 다르던
   0/10/20/25% 탐사율 요구 대신 균일하게 90% 요구하도록 게이트 강화. 몬스터 스펙(V2 밸런스,
   09-18에 미리 채워둔 FLOOR_INFO/MONSTER_INFO)은 이미 라이브에 있어서 DB 작업 불필요했음.
   **→ 라이브에서 91층 진입 자체(계단 게이트, 보스 99층 격파 후 100층 마을 도착)를 아직 실측
   안 함.**
5. **긴급수정**: 위 스케줄러 인터페이스 누락으로 전 사이트 다운 → 인터페이스에 메서드 선언
   추가로 해결.
6. **V2 전투화면 "동료 한 명만 보인다" 재수정**: 처음엔 `#bsPartyRowV2` 마크업만 추가했는데
   (`86931343`), 정작 그걸 채우는 `updateBattlePartyV1()` 호출이 V1일 때만 조건부로 걸려있어서
   V2에선 실제로 한 번도 안 불렸음 -- 두 호출 지점(`updateBattleScreen`/
   `loadPartyAndEquip` 콜백) 모두 버전 무관하게 항상 호출하도록 수정(`70d7f43e`, **아직 라이브
   미검증, 이게 이번 세션 마지막 커밋**).

## 다음에 이어받으면 확인해야 할 것 (순서대로)

1. `git pull` → 빌드 → 재배포 → 서버 정상 기동 확인(`catalina.out`에 "One or more listeners
   failed to start" 없는지).
2. 전투 진입시켜서 V2 화면에 파티원 최대 3명(로스터 줄)이 실제로 보이는지 스크린샷 확인
   (테스트 계정: `일어난다람쥐/카단`, 이동 2~3회면 전투 진입됨, `DICE` 액션 반복 호출).
3. ★7 송곳 제작 → 착용 → 전투에서 "방어력 무시+가산" 효과가 실제로 로그에 찍히는지 확인.
4. 91층 진입 테스트(탐사율 90% 게이트, 99층 보스 격파 후 100층 마을 도착 확인).

## [해결됨, 09-22] 아래는 과거 "보류 중" 기록 -- 실제로는 이미 코드 반영 완료

로스터리 계정 기준 밸런스가 너무 쉽다는 신고 — 원인은 스탯구매(최대 ×2.35)+한계돌파
(최대 ×2.80) 복합 배율이 V2 DEF 캘리브레이션에 반영 안 된 것으로 진단 완료, `DEF_SCALE_FACTOR
≈0.5`로 "투자배율 초과분만" 깎는 방향까지 설계했지만 이 시점엔 코드 미반영이었음.
**→ 09-22에 몬스터 공격력 즉효(ATK 전체 상향)+근본(`investAtkScale`, 투자배율 초과분의
50% 반영) 처방이 실제로 코드 반영됨(상세: `S5_TOWER_DESIGN.md` 09-22 항목들). 더 이상
보류 상태 아님 -- 다만 실측 플레이테스트로 체감 난이도가 적절해졌는지는 아직 미확인.
